{
  "name": "medication-system-v01",
  "private": true,
  "description": "ระบบจัดการยา Google Apps Script — ใช้ npm install แล้วรัน clasp pull",
  "scripts": {
    "postinstall": "node scripts/setup-clasp.js",
    "clasp:login": "clasp login",
    "clasp:pull": "clasp pull",
    "clasp:push": "clasp push --force",
    "clasp:status": "clasp status"
  },
  "devDependencies": {
    "@google/clasp": "^3.1.3"
  }
}
